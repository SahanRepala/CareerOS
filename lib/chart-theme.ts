/** Shared Recharts <Tooltip contentStyle> used across dashboard chart pages. */
export const chartTooltipStyle = {
  borderRadius: 12,
  border: '1px solid hsl(var(--border))',
  background: 'hsl(var(--card))',
  boxShadow: '0 8px 32px rgba(15,23,42,0.10)',
  fontSize: 12,
};
