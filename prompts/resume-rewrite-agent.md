# Resume Rewrite Agent — prompt

**Status:** placeholder. No prompt content yet.

version: 0.0.0-placeholder

## System prompt

(not written yet)

## Notes for whoever writes this

- Keep this file's `version` in sync with the `promptVersion` passed into
  `createPlaceholderAgent` (or the real implementation) in
  `/agents/resume-rewrite-agent/index.ts`.
- Model/provider routing for this prompt belongs in
  `/config/model-routing.config.ts`, not here.
